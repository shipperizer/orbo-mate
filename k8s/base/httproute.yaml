apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: orbo-mate-route
spec:
  parentRefs:
  - name: orbo-mate-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: orbo-mate
      port: 80
